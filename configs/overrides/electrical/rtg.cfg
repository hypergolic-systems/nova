// PB-NUK Radioisotope Thermoelectric Generator. Stock ships a
// `ModuleGenerator` producing 0.75 EC/s. Strip it: the Rust simulator
// owns electrical flow, but RTG isn't ported yet, so the part becomes
// passive mass for now. (When it lands, replace with a Nova module
// that registers a Process producer at `Priority::High`.) Without the
// strip, `ModuleGenerator.FixedUpdate` calls into stock
// `Vessel.RequestResource`, which NREs against our gutted resource
// graph (see `CrossfeedPartSetPatch`).
@PART[rtg]
{
  !MODULE[ModuleGenerator] {}
}
