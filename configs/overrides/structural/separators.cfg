// Stock stack separators are collapsed into NovaDecouplerModule's
// "Full Separation" tweakable — any decoupler can act as a separator,
// so the dedicated Separator_* parts are redundant.
!PART[Separator_0] {}
!PART[Separator_1] {}
!PART[Separator_2] {}
!PART[Separator_3] {}
